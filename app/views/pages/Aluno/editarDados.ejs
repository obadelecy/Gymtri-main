<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Dados</title>
    <link rel="stylesheet" href="/public/css/pag_usuario.css">
</head>
<body>
    <header>
        <a href="/interfaces/interfaceUsuario"><img src="/public/img/logoPng3.png" alt="Logo Gymtri" width="80px"></a>
        <nav id="menu">
            <ul>
                <li><a href="/views/pages/Aluno/buscarProfissional" class="navConfig">Buscar Profissional</a></li>
                <li><a href="/views/pages/Aluno/buscarAcademia" class="navConfig">Buscar Academias</a></li>
                <li><a href="/Produtos/indexprodutos" class="navConfig" target="_blank">Produtos</a></li>
                <li><a href="/views/pages/Aluno/editarDados" class="navConfig">Editar Dados</a></li>
                <li><a href="/index" class="navConfig">Sair</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <div class="container">
            <h2>Configure seu Usuário</h2>
    
            <!-- Visualização e edição do perfil -->
            <div class="profile-pic-container">
                <img id="profile-pic" src="<%= usuario && usuario.foto ? usuario.foto : 'https://via.placeholder.com/100' %>" alt="Foto de Perfil">
                <input type="file" id="file-input" accept="image/*" onchange="previewImage(event)">
            </div>
    
            <div class="form-group">
                <label for="username">Nome de Usuário</label>
                <input type="text" id="username" placeholder="Digite seu nome" value="<%= usuario && usuario.nome ? usuario.nome : '' %>" required>
            </div>
    
            <div class="form-group">
                <label for="altura">Altura</label>
                <input type="text" id="altura" placeholder="Digite sua altura" value="<%= usuario && usuario.altura ? usuario.altura : '' %>" required>
            </div>

            <div class="form-group">
                <label for="peso">Peso</label>
                <input type="text" id="peso" placeholder="Digite seu peso" value="<%= usuario && usuario.peso ? usuario.peso : '' %>" required>
            </div>
    
            <div class="form-group">
                <label for="meta">Minha meta</label>
                <textarea id="text" placeholder="Digite sua meta" rows="3"><%= usuario && usuario.meta ? usuario.meta : '' %></textarea>
            </div>
    
            <button onclick="saveProfile()">Salvar Alterações</button>
        </div>

    </main>

    <script src="/public/js/pag_usuario.js"></script>

</body>
</html>